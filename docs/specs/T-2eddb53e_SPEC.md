# SPEC: T-2eddb53e

## Objective
Add a new documentation file docs/reports/DEFAULT_PROFILE_RUNBOOK.md that explains how to run YBIS with the default profile. Include prerequisites (Ollama running, Python deps), exact commands to run a task, and notes on expected gate outcomes. Keep it concise but complete.

## Requirements
- Implement the objective exactly as stated.
- Keep changes scoped to the identified files.

## Acceptance Criteria
- Required files are created or updated.
- Verifier passes under the active profile.

## Constraints
- Respect policy gates and protected paths.

## Files
- `docs/reports/DEFAULT_PROFILE_RUNBOOK.md`
