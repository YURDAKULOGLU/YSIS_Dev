# SPEC: T-878ce458

## Objective
Update docs/reports/DEFAULT_PROFILE_RUNBOOK.md by adding a short Qdrant (local vector store) setup section. Include docker run command and how to check it is running. Keep it concise.

## Requirements
- Implement the objective exactly as stated.
- Keep changes scoped to the identified files.

## Acceptance Criteria
- Required files are created or updated.
- Verifier passes under the active profile.

## Constraints
- Respect policy gates and protected paths.

## Files
- `docs/reports/DEFAULT_PROFILE_RUNBOOK.md`
