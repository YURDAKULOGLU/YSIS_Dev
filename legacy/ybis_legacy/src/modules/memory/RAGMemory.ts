export class RAGMemory {
    constructor() {}

    retrieve_information(query: string): string {
        // RAG Memory ile ilgili bilgi alma mant buraya eklenecek
        return `RAGMemory'dan alnan bilgi: ${query}`;
    }
}
