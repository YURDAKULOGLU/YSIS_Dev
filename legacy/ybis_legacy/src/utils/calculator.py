class Calculator:
    def add(self, a: float, b: float) -> float:
        """ki sayy toplar ve sonucu dndrr.

        Args:
            a (float): lk say.
            b (float): kinci say.

        Returns:
            float: ki saynn toplam.
        """
        return a + b

    def subtract(self, a: float, b: float) -> float:
        """lk sayyndan ikinci sayy karr ve sonucu dndrr.

        Args:
            a (float): lk say.
            b (float): kinci say.

        Returns:
            float: ki say arasndaki fark.
        """
        return a - b
