export class RAGMemory {
    constructor() {}

    retrieve_information(query: string): string {
        // RAG Memory ile ilgili bilgi alma mantığı buraya eklenecek
        return `RAGMemory'dan alınan bilgi: ${query}`;
    }
}
